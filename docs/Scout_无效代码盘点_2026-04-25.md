# Scout 无效代码与幽灵设计盘点

> 创建: 2026-04-25 by subagent
> 范围: agents/ + infra/ + utils/ + contracts/ + knowledge/ + config.yaml + 蓝图 v1.55-v1.69
> 约束: 只读, 不动代码, 不动数据
> 主要方法: import 计数 / SELECT 命中 / 表 row count / 蓝图明文 vs 代码 grep

工作目录: `D:\13700F\Scout\.claude\worktrees\musing-lehmann-772a01\`

---

## 元数据 (2026-04-25 收尾添加)

- **生成方式**: subagent 全量扫描 32+ Python 文件 + DB schema + 蓝图 + config
- **生成耗时**: 约 15 分钟
- **总条目**: 79 (A:50 真死代码 / B:9 重复实现 / C:20 幽灵设计) + 6 已登重复
- **分类标准**:
  - A 真死代码 = 0 引用 + 0 数据 + 不在路线图
  - B 重复实现 = 同一职责多套实现
  - C 幽灵设计 = 蓝图设计但代码完全未实装

### 健康 check 结果 (主 agent verify)

- ✅ 三类计数对账: A=50, B=9, C=20, 合计 79 (与 subagent 报告一致)
- ✅ "登新 TD" 推荐: 41 条 (subagent 说 35+ 是低估)
- ✅ 引用质量抽样 (5/5): 4 条完全准确 (A4 raw_link / A22 gap_reversal_signals / C1 industry_chain / A36 last_analyzed_at)，1 条小瑕疵 (A29 蓝图 line 6595 是决策清单表头不是字段说明，但"benchmark_code 0 SELECT"结论准确)
- ✅ "零引用" 二次 verify (3/3): A11/A12/A36 broad-scope grep 全部归零

## 处理方式 (2026-04-25 决定)

**不今晚处理**。这份报告作为 **4+3 机制 (蓝图实战校验台账)** 的初始燃料。

- **A 类 (50 条)**: 清理工作, 不进 4+3 台账。下次会话清醒头脑时 final review 后批量处理 (删除 / 登新 TD)。
- **B 类 (9 条)**: 多套实现选一条主实现, 其他归并 / 废弃。进 4+3 台账作 "已 verify 偏差"。
- **C 类 (20+6 条)**: **4+3 台账的初始燃料**。每条蓝图规则都要 verify "前提是否在实战成立"，不成立时调蓝图 / 加前提 / 推迟实装。

## 与 4+3 机制的关系

**元规则** (2026-04-25 写入 [docs/Scout_新会话入场.md](Scout_新会话入场.md) 第 7 条):
> "蓝图内容都要对比实际情况"

这份盘点是元规则的**一次大规模应用**，79 条都是"代码 vs 蓝图 vs 实战"的偏离实例。

下次会话初始化 4+3 台账时, **B + C = 29 条直接进 ⏳ 待 verify 队列**。不预设"代码该对齐蓝图", 每条 verify 后再决定动代码 / 调蓝图 / 推迟。

---

## 一、类别 A: 真死代码

### A.1 真零引用模块 / 类（按"production 是否实例化"判定）

| 编号 | 位置 | 证据 | 风险 | 建议 |
|---|---|---|---|---|
| **A1** | `agents/gate_agent.py` 整文件 (418 行 / 16 KB) | 全仓 grep `gate_agent` 命中 9 次：仅 `tests/test_gate_agent.py` + 文档/CLAUDE.md/AGENTS.md。**无任何 main.py / mcp_server.py / 其它 agent import GateAgent**。recommendation_agent.py 第 760 行做"GateAgent 加分"是**自己写 inline SELECT 替代**，未实例化 GateAgent。 | 低（删 GateAgent 不影响任何运行路径），但删动 `Authority` enum + `SignalScore` dataclass 同步丢失 | **保留-暂用**。`recommendation_agent.py:_phase3_verify` 把 GateAgent 的"同行业近 30 天信号 ≥ 5 → +5"用内联 SQL 重做了一遍。两个选择：(a) 把 GateAgent 删除 + 把 `Authority` 权重表迁到 utils/，(b) 反过来让 recommendation_agent 调用 GateAgent。决定前先**登 TD**：明确 v1.07 之后 GateAgent 是否还有未来 用途。 |
| **A2** | `agents/gate_agent.py::class Authority(Enum)` + `class SignalScore(dataclass)` | grep `Authority\.` / `SignalScore\b` 仅在 gate_agent.py 自身 + tests 命中 | 低 | 跟 A1 一并处理 |
| **A3** | `infra/dashboard.py::format_dashboard_as_text` (124 行附近) | 命中点：tests/test_dashboard.py + scripts/test_dashboard_real.py。**main.py / agents/ / infra/mcp_server.py 全部零引用。** | 低（生产报告走 mcp tool 内的 `_assemble_dashboard` 直接 dict 输出） | **待用户判断**。看似为"人读"格式化保留；如果不再有 CLI 命令调用，建议删（保留 _assemble_dashboard 即可）。 |

### A.2 init_db.py 字段定义但 agents/ 零 SELECT（ghost columns）

> **方法**: grep 字段名（排除 init_db.py + tests + __pycache__），命中 0 次即列入。已在 TD-013 列出的 industry_dict 5 列不重复登。

#### info_units 表（11 列零读，不在已登 TD 中）

| 编号 | 字段 | 证据 | 风险 | 建议 |
|---|---|---|---|---|
| **A4** | `info_units.raw_link` | 0 次 SELECT/UPDATE；`infra/collector.py:91-95` INSERT 也未列入该字段 | 中（信源原 URL 现在没人能查到） | **登新 TD**。"信源溯源"是异常可见性原则的需要；要么去掉，要么把 collector 实际写入 |
| **A5** | `info_units.layer_trace` | 0 次 SELECT；INSERT 也未写 | 低 | 待用户判断（蓝图 v1.51 概念，未实施） |
| **A6** | `info_units.markdown_path` | 0 次 SELECT；蓝图 v1.51 知识层 wiki 编译相关，Phase 3+ | 低 | **保留-未来用**（Phase 3+ wiki 编译） |
| **A7** | `info_units.analysis_fields` (JSON) | 0 次 SELECT；INSERT 也未写 | 低 | 待用户判断 |
| **A8** | `info_units.is_secondary_source` + `independent_confirmation` | 0 次 SELECT/UPDATE | 中（"独立确认"是 v1.48 信号交叉验证的核心字段，蓝图 line 5335-5360 明示） | **登新 TD**（蓝图 v1.48 信号交叉验证未实装，关联 C 类 C7） |
| **A9** | `info_units.weight` (默认 100) | 0 次 SELECT/UPDATE；INSERT 不写 | 低 | 蓝图未明确，建议删 default 100 这种"看似有用其实没人读"字段 |
| **A10** | `info_units.verified` (默认 0) | 0 次读写；蓝图 v1.51 知识库卫生制度（line 5468）描述 verified 概念，但代码未实施 | 中 | **登新 TD**（蓝图 v1.51 manual 信源验证机制未实装） |
| **A11** | `info_units.priority` | 0 次 SELECT/UPDATE | 低 | 待用户判断 |
| **A12** | `info_units.status` (默认 'pending') | 0 次（注：`status='done'/'pending'` 命中 129 次但**全部是 queue.db.message_queue 的字段**，与 info_units 无关） | 中（既然有 pending/processing/done/failed 设计但从不流转，是**"假状态机"**） | **登新 TD**（蓝图 line 3066 "信息状态追踪"未实装；INSERT 静态写 default） |
| **A13** | `info_units.source_type` (默认 'system') | 0 次 SELECT；INSERT 不写 | 低 | 同 A11 |
| **A14** | `info_units.category` | grep 8 文件，但 `agents/signal_collector.py` 写入端有，**`agents/` 全表零 SELECT/WHERE**（仅在 dashboard / mcp_server 透传） | 低 | 待用户判断 — 蓝图描述存在 8 大类，但 Agent 端不基于 category 路由 |

#### watchlist 表（14 列零读，不在 TD-013/-018 中）

| 编号 | 字段 | 证据 | 风险 | 建议 |
|---|---|---|---|---|
| **A15** | `watchlist.industry_name_en` + `industry_name_ko` | 0 次（DB 22 行全 NULL） | 低 | 待用户判断 — 国际化字段；Phase 1 不需要可保留 schema |
| **A16** | `watchlist.priority_score` (REAL) | 0 次（DB 全 NULL） | 中（蓝图 v1.50 line 4716 "行业优先级排序"明示该字段；推送频率/digest 排序应该读它） | **登新 TD** — 蓝图 line 4716 行业优先级机制未实装 |
| **A17** | `watchlist.verification_date` + `verification_changed_at` | 0 次（DB 全 NULL） | 低 | 待用户判断 — `verification_status` 字段被读，但**变更时间永远不写**，意味着无法做 "verification 漂移监控" |
| **A18** | `watchlist.industry_stage` | 0 次（DB 全 NULL）；蓝图 line 2607 "行业阶段升级（萌芽→成长/成长→成熟）"明示 | 高 | **登新 TD** — 蓝图 v1.62 已跟踪标的持有期保护规则依赖该字段判断 stage 升级 |
| **A19** | `watchlist.policy_risk` (默认 'none') | 0 次 SELECT；DB 22 行全 'none' | 高（已在 47 项盘点 EM-07 / **TD 候选 C5** 提及） | **同 TD-NEW-E (C5)** policy_risk='fatal' 自动写入链路缺失 |
| **A20** | `watchlist.motivation_duration` | 0 次（DB 全 NULL） | 中（蓝图 v1.52/v1.54 line 3354 评分公式以 motivation_duration 为基础） | **登新 TD** — v1.52 motivation_duration 评分维度未实装 |
| **A21** | `watchlist.motivation_updated_at` | 0 次 | 低 | 同 A17 |
| **A22** | `watchlist.gap_reversal_signals` | 0 次（DB 全 NULL）；蓝图 v1.50 line 3300-3307 明示 4 种缺口的反转监控 | 高（**整套缺口反转监控完全缺席**） | **登新 TD** — 蓝图 v1.50 gap_reversal 监控 Job 未实装 |
| **A23** | `watchlist.gap_fillability_evidence` + `gap_fillability_updated_at` | 0 次（DB 全 NULL，evidence 也全 NULL） | 低 | 待用户判断 |
| **A24** | `watchlist.penetration_rate` + `penetration_history` + `penetration_source` | 0 次（DB 全 NULL）；蓝图 v1.50 line 3290 "渗透率缺口" core dimension | 高（缺口识别 4 类之一完全废弃） | **登新 TD** — 蓝图 v1.50 渗透率缺口 V1 自动计算未实装 |
| **A25** | `watchlist.thesis` | 0 次 SELECT（DB 仅半导体设备 1 行有内容，21 行 NULL） | 低 | 待用户判断 — 字段是用户审核录入用的，不阻塞 |
| **A26** | `watchlist.key_metrics` (JSON) | 0 次（DB 全 NULL） | 低 | 待用户判断 |
| **A27** | `watchlist.kill_conditions` (JSON) | 0 次 SELECT；scripts/update_semi_eq_5fields.py 写过半导体设备 | 中（"反决定触发条件"如果系统自检无人查，等于摆设） | **登新 TD** — 蓝图 line 6606 "退出信号"未跟踪 watchlist.kill_conditions |
| **A28** | `watchlist.thesis_status` (默认 'intact') | 0 次 SELECT/UPDATE | 中（蓝图 v1.51 推荐质量衡量依赖论点失效检测） | **登新 TD** |

#### industry_dict 表（6 列零读，不在 TD-013 中）

| 编号 | 字段 | 证据 | 风险 | 建议 |
|---|---|---|---|---|
| **A29** | `industry_dict.benchmark_code` | 0 次 SELECT；蓝图 v1.62 "已跟踪标的估值水位提醒"用 benchmark 计算分位数；蓝图 line 6595 `benchmark_index_code` 范围校验 | 高（PE/PS 历史分位数无 benchmark 锚） | **登新 TD** — 蓝图 v1.62 估值水位提醒缺 benchmark_code 数据 |
| **A30** | `industry_dict.data_source_mapping` (JSON) | 0 次 SELECT；蓝图 v1.66 line 1153 "data_source_mapping 100% LLM 可自动匹配" | 中 | **登新 TD** — v1.66 LLM 预填模式数据通道未启用 |
| **A31** | `industry_dict.global_leaders` (JSON) | 0 次 SELECT；蓝图 v1.66 line 1146 "global_leaders 列表 LLM 完全胜任" | 中 | **登新 TD** — 同 A30 |
| **A32** | `industry_dict.historical_cycles` (JSON) | 0 次 SELECT；蓝图 v1.55 line 4595-4617 "复活方向 historical_cycles≠[] 时 why_different_now 必填"是 hard-rule | 高 | **登新 TD**（与 V155-04 关联，已在 47 项盘点登记） |
| **A33** | `industry_dict.historical_context` | 0 次 SELECT | 低 | 待用户判断 |
| **A34** | `industry_dict.why_different_now` | 0 次 SELECT；蓝图 V155-04 已登 — 字段在但卡控逻辑零 | 高 | 同 A32 |

#### related_stocks 表（6 列零读）

| 编号 | 字段 | 证据 | 风险 | 建议 |
|---|---|---|---|---|
| **A35** | `related_stocks.discovery_detail` | 0 次 | 低 | 待用户判断 |
| **A36** | `related_stocks.last_analyzed_at` + `analysis_result` | 0 次 SELECT/UPDATE | 中（推荐结果"上次分析时间"字段从未维护） | **登新 TD** — 推荐缓存 / 分析时效跟踪缺失 |
| **A37** | `related_stocks.gate_block_reason` | 0 次 SELECT/UPDATE | 中（GateAgent 早 deprecate 了，列名也指向死 Agent） | 跟 A1 一并处理 |
| **A38** | `related_stocks.consecutive_fail_count` (默认 0) | 0 次 SELECT/UPDATE | 中（"分析失败计数"是异常可见性原则的核心机制） | **登新 TD** |
| **A39** | `related_stocks.company_policy_risk` (默认 'none') | 0 次 | 高（与 A19 / TD-NEW-C5 同类） | 跟 A19 一并处理 |

#### track_list 表（7 列零读）

| 编号 | 字段 | 证据 | 风险 | 建议 |
|---|---|---|---|---|
| **A40** | `track_list.tier`, `recommend_date`, `recommend_price`, `risk_updated`, `composite_score`, `target_price_auto` | 0 次 SELECT；DB 0 行（**整张表全空**） | 低（Phase 2A 启用时再填） | **保留-未来用**。但建议明确 Phase 2A 谁来 INSERT track_list（目前 cold_start.py + tests 写过样本，但 main.py 调度无 track_list 写路径） |

#### stock_financials 表（4 列零读 — `roe`/`dupont_data`/`historical_peak`/`historical_peak_date`）

| 编号 | 字段 | 证据 | 风险 | 建议 |
|---|---|---|---|---|
| **A41** | `stock_financials.roe` | 0 次 SELECT；agents/master_agent.py 用 roa 而非 roe | 中（财务 5 大师评分蓝图本应有 roe 项） | **登新 TD** — master_agent ROE 项未实装 |
| **A42** | `stock_financials.dupont_data` (JSON) | 0 次 | 低 | 待用户判断 |
| **A43** | `stock_financials.historical_peak` + `historical_peak_date` | 0 次 SELECT/UPDATE | 中（"历史峰值"在估值水位提醒里很关键） | **登新 TD** — 关联 A29 估值水位提醒 |

#### recommendations 表

| 编号 | 字段 | 证据 | 风险 | 建议 |
|---|---|---|---|---|
| **A44** | `recommendations.mode_since` | grep 1 次（`agents/recommendation_agent.py:969-974`），但**写入时与 `recommended_at` 用同一个 ts**，意味着列**永远等于 recommended_at**，没有"模式切换时间"语义 | 中（DB 230 行 mode_since 全部=recommended_at，列冗余） | 同 TD-022 已登 — 但可补充 "mode_since 字段语义未实现" |

#### rules 表整张表零行 + 字段全部零用

| 编号 | 位置 | 证据 | 风险 | 建议 |
|---|---|---|---|---|
| **A45** | `rules` 整张表 + `rule_name`/`rule_type`/`apply_count`/`success_count`/`reliability` 5 列 | DB 0 行；grep `FROM rules\b/INSERT INTO rules` 命中 0；只在 `phase1_acceptance.py` 期望表清单出现 | 中（蓝图 v1.51 line 5496 "程序记忆独立存储在 knowledge.db 的 rules 表中" — **核心程序记忆机制完全未实装**） | **登新 TD** — 蓝图 v1.51 程序记忆 + 规则进化机制未实装。当前 `rules` 表是空摆设 |

#### 其它表（已在 TD 列表，引用即可）

| 表 | 行数 | 状态 | 已登 TD |
|---|---|---|---|
| `industry_chain` | 0 | grep 0 INSERT/SELECT | **登新 TD** — 蓝图 line 6046-6103 "产业链关系图"是知识层核心组件，整张表空 |
| `event_chains` | 0 | 0 SELECT | 蓝图 v1.48 / v1.59 关联机制；属未来阶段（CLAUDE.md 提及） |
| `motivation_drift_log` | 0 | 0 INSERT；MotivationDriftAgent 不写历史日志 | **登新 TD** — Agent 名字带 log 但实际不写 log 表 |
| `user_decisions` | 0 | 0 INSERT | 已登 TD-008 |
| `price_tracking` | 0 | 0 INSERT/SELECT | 蓝图 v1.61 推荐→复盘闭环；属 V161-03 已登 |
| `review_results` | 0 | 0 INSERT/SELECT | 同上，Phase 4 |
| `rejected_stocks` | 0 | 0 INSERT 自动；scripts 测试写 | 蓝图 line 6606，候选未自动写入 |
| `global_companies` | 0 | 0 INSERT/SELECT | 已登 TD-019 |
| `info_industry_map` | 0 | 0 INSERT 生产；只 test 写 | 已登 TD-018b |

### A.3 config.yaml 字段无人读

| 编号 | config.yaml 节 | 字段 | 证据 | 风险 | 建议 |
|---|---|---|---|---|---|
| **A46** | `llm.*` 8 字段：`daily_cost_limit_krw` / `monthly_cost_limit_krw` / `daily_llm_cost_usd_running` / `monthly_llm_cost_usd` / `emergency_stop_single_call_usd` / `emergency_stop_daily_usd` / `api_key_required` / `phase1_mode` | grep 命中 = pydantic schema + tests 之外 0 次 | 低（Phase 1 占位） | **保留-未来用**。蓝图 v1.65/v1.66 USD 预算机制；Phase 3 接 Sonnet 时再 wiring。但建议在 config.yaml 加注释 "Phase 1 不读"，**避免误以为已生效** |
| **A47** | `error_handling.*` 整节：`max_retries`/`retry_backoff_base`/`alert_webhook` | grep `cfg.error_handling` 命中 0（仅 tests/test_config.py:95 isinstance 检查） | 低 | **同 A46** — 配置过 schema 但代码不读；BaseAgent 用类常量 `MAX_RETRIES=3` |
| **A48** | `concurrency.*` 整节：`max_workers`/`db_lock_timeout`/`transaction_isolation` | grep `cfg.concurrency` 命中 0 | 低 | 同 A46。`infra/db_manager.py` timeout 写死 30.0 秒 |
| **A49** | `testing.*` 整节：`coverage_target`/`fixture_dir` | grep `cfg.testing` 命中 0 | 低 | 同 A46 |
| **A50** | `llm.cloud_model` (`claude-sonnet-4-6`) | grep 命中 = pydantic schema + 注释 + tests 0 次实读 | 低 | 同 A46 — Phase 1 占位 |

### A.4 6 个月+无改动且零引用的文件

未发现。仓库 git history 浅（最早 commit 2026-04-18），所有文件最近一次 commit 都在过去 ~一周内。

---

## 二、类别 B: 重复实现

| 编号 | 重复内容 | 位置 1 | 位置 2 | 风险 | 建议 |
|---|---|---|---|---|---|
| **B1** | Ollama helper 三件套（`_call_gemma_text` / `_extract_content` / `_extract_tokens` / `_is_connection_error`） | `agents/signal_collector.py:228-260`（私有静态方法） | `utils/llm_client.py:271-298`（v1.48 抽象层）；`agents/direction_judge.py` + `agents/direction_backfill_agent.py` 已迁到 utils.llm_client | **CLAUDE.md TD list 已记录**（第 5 节第 1 条） | 把 SignalCollector 也迁到 utils.llm_client 即可（待 v1.48 Phase B 时一起做） |
| **B2** | content preview 抽取逻辑 | `infra/dashboard.py::_extract_content_preview` (line 279) | `infra/mcp_server.py::_content_preview` (line 891 — 函数注释明示"与 infra/dashboard._extract_content_preview 行为一致") | 低（行为可能漂移）— **作者已明确知道是重复** | 把 `_extract_content_preview` 公开化（去掉前导下划线），mcp_server 直接 import |
| **B3** | "restrictive 关键词清单"三套并存 | `agents/signal_collector.py::RESTRICTIVE_HARD = ("禁止","取缔","不得","限制","整改","严禁")` | `agents/recommendation_agent.py:520 restrict_kw = ["严控","退出","清退","整治","限制","禁止","取缔","停止","淘汰"]` + `agents/bias_checker.py:63-65 ["禁止","限制","整改","罚款","警告","暴跌","退市","取缔","严禁"]` | 中（三套语义层次混乱：硬约束 vs scoring keyword vs bias warning — 关键词重叠但侧重不同） | **登新 TD**：抽到 `utils/policy_keywords.py`，明确三个等级（HARD / SCORING / WARNING）并加注释；目前任一组改动都需要人记住"另两个地方"漂移没漂移 |
| **B4** | "funded 关键词清单"两套 | `agents/motivation_drift_agent.py:69 FUNDED_KEYWORDS = ["专项资金","补贴","拨款","财政支持","重大专项"]` | `agents/recommendation_agent.py:515-516 funded_kw = ["财政","专项","补贴","基金","投入","资金","万亿","亿元","预算","拨款","财政支持","重大专项","专项资金"]` | 中（v1.61 风控 P0-P4 推送依赖 funded 等级 — 两套不同，可能造成 motivation_drift_agent 判断 reversing 但 recommendation_agent 不识别） | **同 B3** — 抽到 `utils/policy_keywords.py` 统一 |
| **B5** | "GateAgent 同行业信号 ≥5 条 +5" 评分逻辑两套 | `agents/gate_agent.py::generate_report` (production never wired) | `agents/recommendation_agent.py:_phase3_verify` (line 760-780) inline SQL 重做 | 低（功能在 recommendation_agent 实现）— 但是**A1 候选删 GateAgent 的另一面证据** | 跟 A1 合并处理 |
| **B6** | `info_unit_id` 与 collector `make_info_unit_id` | `utils/hash_utils.py::info_unit_id(source,title,date)` | `infra/collector.py::Collector.make_info_unit_id(title,date)`（self.SOURCE_CODE 自带）— 内部是 `info_unit_id(self.SOURCE_CODE, title, date)` 包装 | 低（包装合理，是 dataset-binding） | **保留** — 不算真重复，是 thin wrapper |
| **B7** | Pydantic InfoUnitV1 vs DB schema 字段差异 | `contracts/contracts.py::InfoUnitV1` 9 字段 | `knowledge/init_db.py` info_units 表 23 列 | 中（**契约只校验 9 个，剩 14 列 init 时 default 后无人写**），既是 A 类幽灵字段又是 B 类设计漂移 | 跟 A4-A14 一并处理 |
| **B8** | bias_checker 与 signal_collector 关键词列表 | bias_checker 第 63 行（同 B3） | signal_collector RESTRICTIVE_HARD（同 B3） | 同 B3 | 同 B3 合并 |
| **B9** | `agents/__init__.py` 存在但 `infra/`、`utils/`、`contracts/`、`knowledge/` 没 `__init__.py` | 5 个包目录中只 1 个有 init | (CLAUDE.md TD list 第 5 节已登) | 低（沿用 sys.path 补丁） | 已登记 |

---

## 三、类别 C: 幽灵设计（蓝图明文，代码无对应实装）

> 已被 47 项盘点 / TD 清单覆盖的不重复登；以下是新发现的、47 项之外的（按蓝图章节由前往后）。

| 编号 | 蓝图位置 | 设计内容 | 实装状态 | 已登 TD？ | 建议 |
|---|---|---|---|---|---|
| **C1** | line 6093-6103 "产业链关系图" | `industry_chain` 表 5 类关系（上游/下游/配套/替代/互补）+ 关系失效机制（6m/6-12m/12m+ 三档）+ Agent 沿关系图扩散分析 | ❌ — 表 0 行；零 INSERT；零 SELECT；蓝图 line 6097 "Agent 如何使用"4 类 Agent 全部未对接 | 否 | **登新 TD（高优先级）** — 蓝图 6 章节明文要求知识层"核心组件" |
| **C2** | line 4734-4778 "论文周报机制" | 推送规则（动机驱动行业 ≥1 篇 + 政策行业 + 复盘提及）+ 不推送规则（无 confidence > 80% / 仅外刊 / 已扫过的事件） | ⚠️ — `agents/direction_judge.py` weekly_paper_report 存在，但**推送规则的"不推送过滤"未实装**（zero-impact 论文照推） | 否 | **登新 TD（中优先级）** |
| **C3** | line 5424-5459 "research 模式报告格式" + line 5472 "默认只返回权威+可靠信源" | 检索 "问 XX" 默认过滤 source_credibility="参考"+"线索"；要 "问 XX 全部" 才显示 | ❌ — `infra/mcp_server.py::ask_industry/get_industry_full_context` 未做 credibility 过滤 | 否 | **登新 TD（中优先级）** — 蓝图明示但 MCP 工具未实施 |
| **C4** | line 2621-2647 "Scout 自知力检查（7 层盲区检测）" | 7-8 层盲区（字段缺失/信源未接/行业遗漏/公司遗漏/关键词遗漏/数据过时/规则错误/信息维度盲区）+ 周报自知力摘要 | ❌ — 周报无"自知力摘要"段；7 层检测全无代码 | 否 | **登新 TD（高优先级）** — Scout 异常可见性原则的直接落地点；缺它就是"系统不知道自己不知道什么" |
| **C5** | line 2586-2615 "config.yaml 范围校验" + "已跟踪标的估值水位提醒" | (a) 启动时 + 变更时校验 config 参数范围；超出 → 拒绝启动+推送 (b) tracking 标的 PE/PS 历史分位数 >80%/>95% 推送 | ❌ — `config/loader.py` 仅 Pydantic schema 校验类型；没有数值范围 + min/max 校验；估值水位提醒 0 实装 | 否 | **登新 TD** — (b) 部分跟 A29 + A43 关联 |
| **C6** | line 2627-2641 "信息维度盲区（Phase 3+）" | 每个行业标注 Scout 覆盖了什么维度（政策✅/科研✅/宏观数据✅/公司财报✅/管理层动态❌/竞品情报❌/消费者端❌），推荐时附带 | ❌ — `agents/recommendation_agent.py::_render_report` 报告结构无"覆盖维度披露"段；info_units 也没"维度"字段 | 否 | **登新 TD（低优先级，蓝图自标 Phase 3+）** |
| **C7** | line 5335-5360 "信号交叉验证时间窗口" | info_units 双信源/独立确认机制：`is_secondary_source` + `independent_confirmation` 字段 + 跨信源交叉验证窗口（3d/7d/30d） | ⚠️ — schema 字段在但零写零读（A8 提及）；交叉验证逻辑零代码 | 否 | **登新 TD** — 同 A8，蓝图 v1.48 机制完全空 |
| **C8** | line 1338-1457 "Demo 模式" (v1.64) | `scripts/demo_mode.py` 1 个预置行业 + 完整字段 + 推荐 + 10 步引导脚本 | ❌ — 文件不存在；演示链路 0 实装 | 否 | **保留-未来用**（蓝图明示 Phase 2A 完成后开发） |
| **C9** | line 1136-1335 "冷启动 LLM 预填模式" (v1.66) | `scripts/cold_start_llm_prefill.py` + Claude Code 自动调用 LLM 填 14 类字段（global_leaders/sub_industries/aliases/...） | ❌ — 文件不存在；目前 cold_start.py 是手动模式 | 否 | **保留-未来用**（蓝图 Phase 2B+） |
| **C10** | line 421 "Scout MCP Server 给 LLM 的工具" — `get_review_analysis_data` MCP tool | 复盘归因数据接口：`recommendations_in_window` + `hit_rate_breakdown` + `attribution_raw_data` + `decision_history` | ❌ — `infra/mcp_server.py` 17 个 @app.tool 无该 tool；grep 0 命中 | 否 | **登新 TD（中优先级）** — Scout-LLM 协作架构 v1.62 4 个核心 MCP tool 中第 4 个未实装 |
| **C11** | line 437-448 `get_industry_full_context` blueprint vs implementation gap | 蓝图说该工具应返回 7 个字段（含 `historical_events`/`past_recommendations`/`sub_market_signals`/`scout_uncovered_dimensions`） | ⚠️ — `infra/mcp_server.py:574-590` 实现只返 3 个 (`watchlist_entry`/`recent_info_units_180d`)；其它 4 个全部 `[]` 占位 + `phase_notes` 注明 "Phase 2A 填充" | 否 | **登新 TD** — Phase 2A 收尾任务，跨表 join 都未做 |
| **C12** | line 451-459 `get_decision_context` blueprint vs implementation gap | 蓝图说该工具应返回 5 个字段（含 `recommendation`/`dimensions_detail`/`industry_context`/`user_past_decisions_similar`/`comparable_stocks`） | ⚠️ — `infra/mcp_server.py:783-807` 仅返 `related_stocks_entries[]`；其它 4 个未实装 | 否 | **登新 TD** — 同 C11 |
| **C13** | line 5496-5535 "三类记忆"（情景/语义/程序）+ rules 表 JSON schema | 程序记忆 rules 表用 `rule_id/created/source/type/target/content/reason/supersedes/auto_apply/status` 结构化 JSON | ⚠️ — 表存在但 0 行 + DB schema 是**扁平的 rule_name/rule_type/apply_count/success_count/reliability**（非 JSON 结构）；蓝图设计的 supersedes / auto_apply 字段缺失 | 否 | **登新 TD** — A45 已列；这条额外说明 schema **设计与蓝图不一致**（不仅是空，是结构错） |
| **C14** | line 1041-1042 "API 成本告警 100%/150%/200%" + line 2580-2584 "₩4500/₩15000 告警条件" | 单日 / 单周 / 单月 三级 API 成本告警 + 自动停 Sonnet | ❌ — `agents/health_monitor_agent.py` 检查推送堵塞，**不监控 LLM 成本**；`llm_invocations.cost_usd_cents` 列在但无 Job 聚合 | 否 | **登新 TD** — 47 项盘点 EM-06 已提，但 EM-06 描述较粗，可补充更具体 |
| **C15** | line 2654-2700 `models:` map + line 2592-2599 `config_validation:` map | config.yaml 顶层应有 `models:` 节（agent→model 显式映射）+ `config_validation:` 节（参数 min/max） | ❌ — config.yaml 当前不存在这两节；`config_validation` 也没在 loader.py 实装 | 否 | **登新 TD** — 蓝图 v1.62 配置约定未对齐 |
| **C16** | line 3437-3461 "动机降级预警机制" 完整 6 条触发条件 | 动机维度 ↓ / 政策方向反转 / 关键词情绪反转 / 财务恶化 / 信号冲突 / 时间衰减 — 6 条触发任一 → 推送 🟡 / 🔴 | ⚠️ — `motivation_drift_agent` 实现了 4 类信号（A/B/C/D 政策/关键词/财务/冲突），但**6 条降级触发条件不齐**；推送链路也未对齐 P0-P4 | 否 | **47 项盘点 EM-04 已提**；本条不重复登 |
| **C17** | line 3553-3582 "Scout 的'自认失败'条款" L0-L4 状态机 | system_meta 应有 `failure_level` / `failure_since` / `self_report_sent` 字段；L4 持续 6 月推送 🔴 自检报告 | ❌ — `system_meta` 表无这些字段；L0-L4 状态零代码 | 否 | **登新 TD** — 蓝图 v1.53 失败预案核心机制完全缺席 |
| **C18** | line 4645-4651 "readiness 180 天未更新过期推送 🟡" | `industry_dict.readiness_updated_at` 超 180 天 → 推送 🟡 + 用户回复"重新评估" | ❌ — Job 不存在；47 项盘点 EM-08 已提 | 是（EM-08） | 不重复登 |
| **C19** | line 5460-5474 "知识库卫生制度" 6 类垃圾处理 | 知识维护 Agent 每周/每月扫描；过时事实 / 低价值噪音 / 未验证积压 / 错误关联 / 孤儿数据 / 重复信息 → 6 种处理 | ❌ — 知识维护 Agent 不存在；6 类垃圾处理零代码；info_industry_map 表空（TD-018b 已登） | 否 | **登新 TD** — 蓝图 v1.51 知识库卫生制度核心 |
| **C20** | line 4083-4085 "数据安全（物理环境保护）"+ line 4011-4042 "外部依赖风险" | 加密备份 / 多节点 / 外部依赖容错 | ❌ — Phase 1 不在范围 | — | 蓝图自身 Phase 3+，**保留** |

---

## 四、汇总

### 类别 A（真死代码）：**50 条**

- A1-A3：3 个真零引用模块/类（GateAgent + Authority/SignalScore + format_dashboard_as_text）— **预估清理收益最大**：删 GateAgent 可省 418 行 + 16 KB；同时强迫 RecommendationAgent 显式承认它"自己重做了一遍"
- A4-A14：info_units 表 11 列零读（其中 A8 / A10 / A12 是有蓝图设计但未实装）
- A15-A28：watchlist 表 14 列零读（多个有蓝图依据但未实装；A19 / A22 / A24 是 P1）
- A29-A34：industry_dict 表 6 列零读（A29 / A32 是 P1）
- A35-A39：related_stocks 表 5 列零读
- A40-A44：track_list / stock_financials / recommendations 共 8 列零读
- A45：rules 表整张表 + 5 列零用（**蓝图 v1.51 程序记忆机制完全空**）
- A46-A50：config.yaml 5 类节零读（llm 8 字段 + error_handling + concurrency + testing + cloud_model）

**预估清理收益**：
- 直接删（A1+A2 — GateAgent + Authority + SignalScore + format_dashboard_as_text）：约 540 行；不影响任何运行链路
- 不能直接删但应"登 TD + 加注释"明示"占位"的：约 35 个字段 + 4 个 config 节
- 既"清理"也"补登 TD"两手做：A45 (rules 表)、A22 (gap_reversal)、A24 (penetration_*)、A19 (policy_risk='fatal')

### 类别 B（重复实现）：**9 条**

- B1（Ollama helper 三件套）：CLAUDE.md 已登，**架构选择**（v1.48 Phase A 暂保留 SignalCollector 不迁移）
- B2（content preview）：作者注释明示重复 — **直接遗留**，应该清理
- B3 / B4 / B8（关键词列表三套 / 两套）：**单纯遗留 + 漂移风险**，建议抽到 `utils/policy_keywords.py`
- B5（GateAgent 同行业信号评分）：跟 A1 合并
- B6（info_unit_id wrapper）：架构选择，无需动
- B7（Pydantic vs DB schema 字段差异）：跟 A 类合并
- B9（缺 __init__.py）：CLAUDE.md 已登

### 类别 C（幽灵设计）：**20 条新增 + 6 条引用已登**

按蓝图版本/章节分布：

- 蓝图 line 1041-1042 / line 2580-2584 → C14（API 成本告警，跟 EM-06 关联）
- 蓝图 line 1136-1335 (v1.66 LLM 预填) → C9
- 蓝图 line 1338-1457 (v1.64 Demo) → C8
- 蓝图 line 2586-2615 (v1.62 config_validation + 估值水位) → C5 + C15
- 蓝图 line 2621-2647 (Scout 自知力 7 层) → C4
- 蓝图 line 3437-3461 (动机降级预警 6 条) → 引用 EM-04
- 蓝图 line 3553-3582 (Scout 自认失败 L0-L4) → C17
- 蓝图 line 4645-4651 (readiness 180 天过期) → 引用 EM-08
- 蓝图 line 4734-4778 (论文周报推送规则过滤) → C2
- 蓝图 line 5335-5360 (信号交叉验证时间窗口) → C7
- 蓝图 line 5424-5474 (research 模式 + 知识库卫生 + 信源 credibility 过滤) → C3 + C19
- 蓝图 line 5496-5535 (rules 表 JSON schema 三类记忆) → C13
- 蓝图 line 6046-6103 (产业链关系图) → C1
- 蓝图 line 421-479 (4 个 MCP tool) → C10 + C11 + C12

**结论：**蓝图在 v1.48 / v1.50 / v1.51 / v1.53 / v1.54 / v1.62 / v1.64 / v1.66 这 8 个版本之间设计的**至少 14 个独立机制**完全未实装；全部都不是 47 项盘点已经覆盖的内容。

### 总数

- 类别 A: **50 条**（其中 35+ 条**应登新 TD**，3 条可直接删）
- 类别 B: **9 条**（其中 4 条单纯遗留应清理，5 条架构选择/已登）
- 类别 C: **20 条新增**（其中 8 条 P0/P1 — C1 / C4 / C5 / C7 / C13 / C14 / C15 / C17）

---

## 五、最高 ROI 5 条（推荐用户先看）

按 **置信度高 + 处置明确 + 收益明显** 排序：

### 1. **A1+A2 — 删 `agents/gate_agent.py` 整个文件**

- 证据：grep 显示零生产引用，仅 tests + docs；recommendation_agent 自己 inline 重做了"GateAgent +5"逻辑
- 收益：**清掉 418 行未活跃代码**；同时清理 `Authority` enum + `SignalScore` dataclass + `related_stocks.gate_block_reason` 字段（A37）
- 风险：tests/test_gate_agent.py 也要删（约 350 行测试同时删）
- 处置：**登 TD-NEW**："决定删除 GateAgent — recommendation_agent 已替代，Authority 表（如需）抽到 utils/authority.py"

### 2. **A45 — `rules` 表 + 5 列彻底空 ghost**

- 证据：DB 0 行；grep `FROM rules\b/INSERT INTO rules` 命中 0；蓝图 v1.51 line 5496 明示 "程序记忆独立存储在 knowledge.db 的 rules 表中" — **核心程序记忆机制完全未实装**
- 收益：**P0 级 TD 候选**：知识层"三类记忆（情景/语义/程序）"是 Phase 3+ 复盘机制依赖；同时**当前 rules 表 schema 与蓝图 JSON 设计不一致**（C13），不仅空还结构错
- 风险：高 — 不补这条等于把 v1.51 复盘进化机制延后到 Phase 4 都不可能实施
- 处置：**登 TD-NEW（高优先级）**

### 3. **A19 + A22 + A24 — `policy_risk='fatal'` / `gap_reversal_signals` / `penetration_*` 三组 ghost 字段**

- 证据：3 组共 6 字段；DB 全 NULL；蓝图 v1.50（缺口反转）+ v1.61（policy_risk='fatal' 自动移出活跃区，47 项盘点 EM-07）+ v1.50 渗透率缺口都明示 — **代表蓝图 v1.50/v1.61 缺口体系完全形同虚设**
- 收益：与已登 TD-014（d4 weighted）/ TD-021（mixed_subtype）/ TD-024（policy_risk='fatal'）相互印证；补一组就能解锁缺口反转监控 Job
- 处置：**登新 TD**（与现有缺口/政策风控 TD 关联）

### 4. **C1 — `industry_chain` 表 0 行（产业链关系图核心组件未实装）**

- 证据：表存在；DB 0 行；蓝图 line 6046-6103 明示 "知识层核心组件" + 4 类 Agent 沿关系图扩散使用；当前 4 类 Agent 全部不查 industry_chain
- 收益：知识层从"22 个孤立行业"升级为"网络结构"——蓝图反复强调这是 Scout 区别于"散信号库"的核心；不补它**Scout 的差异化优势在知识层完全没体现**
- 处置：**登新 TD（高优先级）**

### 5. **B3 + B4 — 关键词清单三套并存（restrictive / funded）**

- 证据：3 处 `restrictive` 关键词列表 + 2 处 `funded` 关键词列表；漂移风险已客观存在（每个列表内容略不同）
- 收益：抽到 `utils/policy_keywords.py` 后，未来政策语料更新或 v1.61 风控状态机（已登 TD-014/021）实施时**只需改一处**；**异常可见性原则的实现层面**（蓝图 line 5460 知识库卫生制度也指向类似集中管理）
- 风险：低（重构无功能变更，只需迁移 import 路径 + 加单测）
- 处置：**直接登 TD-NEW（中优先级）**，1 PR 解决

---

## 附录：方法论

- import 计数：`grep -rE "(import <name>|from <name> )" --include="*.py" .`，排除 `__pycache__`，统计行数
- 字段使用：`grep -rE "<column>" --include="*.py" .`，排除 `init_db.py` + tests + docs
- 蓝图 vs 代码：先 `grep -n "<keyword>" 系统蓝图.md` 锁定 line range，再 `grep` 代码确认字段/函数是否存在
- DB 行数：`sqlite3 D:/13700F/Scout/data/knowledge.db "SELECT COUNT(*) FROM <table>"`
- Pydantic schema vs DB schema 对照：`Read contracts/contracts.py` + `Read knowledge/init_db.py` 字段一一对照

未做：

- `tests/` 整套测试代码无效性盘点（如有 dead 单测）— 出于报告范围考虑省略
- `prompts/` 目录下文件是否仍被 LLM 实际加载，未深入 grep
- `scripts/` 30+ 个 helper / migration 文件是否过期，未做生命周期审计
- 蓝图 line 3000-4000 段（推送规则、动机标签、政策信源细节等）有约 100+ 个细则未逐条 audit

# Scout 蓝图 Phase 1 决策盘点

> **日期**: 2026-04-25
> **范围**: 蓝图 "决策清单速查" 10 项 Phase 1 前必改决策
> **范围限制**: 本次只盘点 10 项 Phase 1 决策。完整蓝图 (340KB / 6835 行) 全部决策的盘点已由 subagent 完成 47 项，在 [logs/audit/2026-04-25_blueprint_implementation_audit.md](../logs/audit/2026-04-25_blueprint_implementation_audit.md)，待 review 决定是否归档到 docs/。

---

## 10 项决策状态

| # | 决策 | 状态 | 实施位置 | 证据 | 缺口 |
|---|---|---|---|---|---|
| **1** | industry_id 主键 | ⚠️ **部分** | `watchlist.industry_id` (PK) / `related_stocks.industry_id` (FK) / `recommendations.industry_id` / `global_companies.industry_id` | `PRAGMA table_info(watchlist)` → `industry_id INTEGER pk=1` | **不一致**: `industry_dict.industry` 用 TEXT 作 PK，不用 industry_id（→ TD-013 数据孤岛根因之一）|
| **2** | global_company_id 关联 | ⚠️ **部分** | `global_companies` 表存在；`related_stocks` / `track_list` 有 FK 字段 | `global_companies.global_company_id TEXT pk=1` | **表 0 行** + `recommendations` / `stock_financials` / `price_tracking` **未加 FK 字段**。跨市场同公司链路完全没建（蓝图举例 005930.KS / SSNLF 三星共享财务的设计未落地） → **TD-019** |
| **3** | UTC 时间统一 | ✅ 完整 | `utils/time_utils.py` + `contracts/contracts.py:33-42` Pydantic 时区校验器 | `if dt.tzinfo is None: raise...` + `if dt.utcoffset() != timedelta(0): raise...` | — |
| **4** | Pydantic 契约 (extra='forbid') | ✅ 完整 | `contracts/contracts.py` | `extra='forbid'` grep 命中 3 处 (UnitV1 / 各派生模型) | — |
| **5** | 错误传播矩阵 (6 类) | ✅ 完整 | `agents/base.py:27-47` | `ScoutError` 基类 + 5 子类: `NetworkError` / `ParseError` / `LLMError` / `RuleViolation` / `DataMissingError`；未匹配落 unknown fall-through (line 142+) | — |
| **6** | 并发控制 (WAL + BEGIN IMMEDIATE) | ✅ 完整 | `infra/db_manager.py:33,44` | `PRAGMA journal_mode = WAL` (line 33) + `cur.execute("BEGIN IMMEDIATE")` (line 44) | — |
| **7** | 幂等性策略 (info_units.id hash) | ✅ 完整 | `contracts/contracts.py:75` + `info_units.id TEXT PK` | `id: str = Field(..., description="hash(source+标题+发布日期)[:16] hex")` | — |
| **8** | recommendations.mode | ✅ 完整 | `recommendations.mode TEXT DEFAULT 'cold_start'` | 230 行全是 `cold_start` | running / diagnosis / 架构重审 等 Phase 3 模式未启用（本次盘点视为设计如此不算缺口；但 subagent 47 项盘点把这条标 P0，标识 "v1.56 前视偏差 ×0.8 缓解未实施"，详见 logs/audit/） |
| **9** | Prompt 版本化 | ✅ 完整 | `prompts/` 目录 + `prompts/CHANGELOG.md` | 3 个 `_v001` prompt 文件 + CHANGELOG 明示 "Each prompt file is immutable once released" | — |
| **10** | 测试策略 | ✅ 完整 | `tests/` 31 个 unit + `scripts/` 14 个 `test_*_real.py` 真网络烟囱 | CLAUDE.md 报告 1424 passing tests | — |

---

## 总结

**8/10 完成**，2 个缺口：

- **#1 industry_id 主键不一致**（`industry_dict` 用 TEXT PK） → 含在 [TD-013](Scout_技术债务清单.md#td-013--industry_dict-表与-watchlist-数据孤岛--字段功能重复)
- **#2 global_company_id 关联未落地** → 新登 [TD-019](Scout_技术债务清单.md#td-019--global_company_id-关联数据未落地)

其他 8 项（UTC / Pydantic / 错误矩阵 / WAL / 幂等 / mode / Prompt 版本 / 测试）实装扎实，**Scout 工程地基稳**。

---

## 后续待做

- **完整蓝图 (340KB) 全部决策的盘点** ✅ 已由 subagent 完成 47 项，写到 [logs/audit/2026-04-25_blueprint_implementation_audit.md](../logs/audit/2026-04-25_blueprint_implementation_audit.md)
  - subagent 总数: ✅ 8 / ⚠️ 20 / ❌ 15 / ❓ 4
  - subagent 标的 P0 阻塞: D008 (本表 #8 同条) / V161-02 (= TD-014a) / D007 (= TD-017) / V160-02/03/05
  - subagent 发现 7 条 TD 候选 (TD-NEW-A 至 G)，**未登记**，待用户 review
- **TD-013 + TD-019 在 Phase 2A 一起做架构决策时同步推进**
